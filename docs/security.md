# Security Gates

- Store secrets only in environment variables
- Never commit `.env.local` or tokens
- Keep dependencies up to date and monitor advisories
- Validate all inbound user input on server boundaries
