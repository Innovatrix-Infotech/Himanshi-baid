# Preflight Checklist

- Pull latest `development`
- Confirm branch naming is valid for work type
- Install dependencies with `yarn install`
- Copy `.env.example` into `.env.local` and populate values
- Run `yarn lint`
- Run `yarn typecheck`
- Run `yarn test`
